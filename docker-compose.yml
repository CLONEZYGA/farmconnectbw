version: '3.8'
services:
  expo:
    build: .
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - "19000:19000"
      - "19001:19001"
      - "8081:8081"
    environment:
      - EXPO_DEVTOOLS_LISTEN_ADDRESS=0.0.0.0
    command: npm start
